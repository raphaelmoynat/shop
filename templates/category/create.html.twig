{% extends 'base.html.twig' %}



{% block body %}
    {{ form_start(form) }}

        <div class="mb-3">
            {{ form_row(form.name)}}
        </div>

    <button type="submit" class="btn btn-success">Send</button>

    {{ form_end(form) }}
{% endblock %}